<content id="data-content">

    <style>
        .example-themePrimary a {
            color: {{@root.theme.palette.themePrimary}};
        }

        .icon {
            width: 20px;
            height: 16px;
            margin-right: 8px;
        }

        ul.template--custom {
            list-style: none;
            padding-left: 5px;
            margin: 0;
        }

        ul.template--custom li {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 8px;
            border-bottom: 1px solid #ddd;
        }

        .result-col {
            display: flex;
            align-items: center;
        }

        /* ✅ Content column widths */
        .result-title {
            flex: 2;
            min-width: 37%;
        }

        .result-project {
            flex: 1;
            min-width: 15%;
            text-align: left;
        }

        .result-modified {
            flex: 1;
            min-width: 18%;
            text-align: left;
        }

        .result-site {
            flex: 1;
            min-width: 20%;
            text-align: left;
        }

/* Content */
.result-folder {
    flex: 1;
    min-width: 15%;
    display: flex;
    justify-content: center; /* center folder icon */
    align-items: center;
    text-align: center;
}

        .template--header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 10px;
        }

        /* ✅ Header column widths (now match content) */
        .template--columns {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 8px 5px;
            border-bottom: 2px solid #333;
            font-weight: 600;
        }

        .column-title {
            flex: 2;
            min-width: 37%;
        }

        .column-project {
            flex: 1;
            min-width: 15%;
            text-align: left;
        }

        .column-modified {
            flex: 1;
            min-width: 18%;
            text-align: left;
        }

        .column-site {
            flex: 1;
            min-width: 15%;
            text-align: left;
        }

        .column-folder {
    flex: 1;
    min-width: 25%;
    display: flex;
    justify-content: center; /* center header text */
    text-align: center; /* backup for text */
}

        /* Folder link icon styling */
        .result-folder .folder-link {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-size: 18px;
            color: inherit !important;
            text-decoration: none !important;
            border: none;
            background: none;
            padding: 4px;
            line-height: 1;
            vertical-align: middle;
            outline: none;
        }

        .result-folder .folder-link:link,
        .result-folder .folder-link:visited,
        .result-folder .folder-link:hover,
        .result-folder .folder-link:focus,
        .result-folder .folder-link:active {
            color: inherit !important;
            text-decoration: none !important;
            outline: none;
            background: none;
        }

    </style>

    <div class="template">

        {{#if @root.properties.showSelectedFilters}}
            <pnp-selectedfilters
                data-filters="{{JSONstringify filters.selectedFilters 2}}"
                data-filters-configuration="{{JSONstringify filters.filtersConfiguration 2}}"
                data-instance-id="{{filters.instanceId}}"
                data-operator="{{filters.filterOperator}}"
                data-theme-variant="{{JSONstringify @root.theme}}">
            </pnp-selectedfilters>
        {{/if}}

        <div class="template--header">
            {{#if @root.properties.showResultsCount}}
                <div class="template--resultCount">
                    <label class="ms-fontWeight-semibold">
                        {{getCountMessage @root.data.totalItemsCount @root.inputQueryText}}
                    </label>
                </div>
            {{/if}}

            <div class="template--sort">
                <pnp-sortfield
                    data-fields="{{JSONstringify @root.properties.dataSourceProperties.sortList}}"
                    data-default-selected-field="{{sort.selectedSortFieldName}}"
                    data-default-direction="{{sort.selectedSortDirection}}"
                    data-theme-variant="{{JSONstringify @root.theme}}">
                </pnp-sortfield>
            </div>
        </div>

        <!-- ✅ Column header row -->
        <div class="template--columns">
            <div class="column-title">Title</div>
            <div class="column-project">Project.nr</div>
            <div class="column-modified">Modified</div>
            <div class="column-site">Company</div>
            <div class="column-folder">Folder</div>
        </div>

        <ul class="template--custom">
            {{#each data.items as |item|}}
                <li>
                    <div class="result-col result-title">
                        <pnp-iconfile
                            class="icon"
                            data-extension="{{slot item @root.slots.FileType}}"
                            data-theme-variant="{{JSONstringify @root.theme}}">
                        </pnp-iconfile>
                        <span class="example-themePrimary">
                            <a href="{{slot item @root.slots.Path}}?web=1" target="_blank" data-interception="off">
                                {{slot item @root.slots.Title}}
                            </a>
                        </span>
                    </div>

                    <div class="result-col result-project">
                        {{slot item @root.slots.ProjectNumber}}
                    </div>

                    <div class="result-col result-modified">
                        {{getDate (slot item @root.slots.LastModifiedTime) "DD.MM.YY HH:mm"}}
                    </div>

                    <div class="result-col result-site">
                        {{slot item @root.slots.SiteTitle}}
                    </div>

                    {{#if (slot item @root.slots.ParentLink)}}
                        <div class="result-col result-folder">
                            <a href="{{slot item @root.slots.ParentLink}}"
                               target="_blank"
                               data-interception="off"
                               title="Open folder"
                               aria-label="Open folder"
                               class="folder-link">
                                📁
                            </a>
                        </div>
                    {{/if}}
                </li>
            {{/each}}
        </ul>
    </div>

    {{#if @root.properties.paging.showPaging}}
        <pnp-pagination
            data-total-items="{{@root.data.totalItemsCount}}"
            data-hide-first-last-pages="{{@root.properties.paging.hideFirstLastPages}}"
            data-hide-disabled="{{@root.properties.paging.hideDisabled}}"
            data-hide-navigation="{{@root.properties.paging.hideNavigation}}"
            data-range="{{@root.properties.paging.pagingRange}}"
            data-items-count-per-page="{{@root.properties.paging.itemsCountPerPage}}"
            data-current-page-number="{{@root.paging.currentPageNumber}}">
        </pnp-pagination>
    {{/if}}

</content>

<content id="placeholder-content">
    <style>
        .placeholder .icon {
            width: 20px;
            height: 16px;
            margin-right: 5px;
        }

        .placeholder ul {
            list-style: none;
            margin: 0;
            padding: 0;
        }

        .placeholder ul li {
            display: flex;
            align-items: center;
            padding: 8px;
        }
    </style>

    <div class="placeholder">
        <ul>
            {{#times @root.properties.paging.itemsCountPerPage}}
                <li>
                    <div class="icon placeholder--shimmer"></div>
                    <span class="placeholder--shimmer placeholder--line" style="width: 60%"></span>
                </li>
            {{/times}}
        </ul>
    </div>
</content>
